---
description: 检索协议问答:先查库再生成,新知归档
argument-hint: [你的问题]
---

针对问题「$ARGUMENTS」执行检索协议(CLAUDE.md 第三节):

1. **查索引**:读 `30.Resources/_index.md` 及相关子索引定位候选笔记;涉及馆主偏好/习惯的问题查 `memory.md`。
2. **读原文**:打开命中的笔记读实际内容,不靠记忆回答。
3. **提炼输出**:基于读到的内容综合回答;每个关键结论标注来源 `[[wikilink]]`;明确区分「来自知识库」与「大模型补充(非来自库)」。
4. **归档新知**:若回答产生了库中没有的有价值合成(对比、推导、结论),建为 wiki 页存入 `30.Resources/`,并在 `_log.md` 记 `query+store`。
5. **诚实兜底**:库中无相关内容 → 明说「知识空白」,把主题加入 `_index.md` 的待补建列表,再给大模型基础解释并声明其来源性质。
